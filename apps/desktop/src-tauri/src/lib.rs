//! Private Code Desktop — Tauri 2 library crate.
//!
//! The `lib.rs` is the crate root that Tauri's `cdylib` links against.
//! The `main.rs` is the binary entrypoint.

pub mod commands;
pub mod state;
